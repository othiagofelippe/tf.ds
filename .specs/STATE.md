# STATE — tf.ds

## Decisions

| ID     | Decision                                                                                                                                                                                                                                                                                                                                          | Status | Date       | Rationale                                                                                                      |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ---------- | -------------------------------------------------------------------------------------------------------------- |
| AD-001 | Inspiração no Alchemy (Livelo) é conceitual — nunca copiar código (IP da empresa)                                                                                                                                                                                                                                                                 | active | 2026-07-11 | Legal/ético; o valor está nos padrões aprendidos                                                               |
| AD-002 | DS exige Tailwind v4 do consumidor (classes semânticas do preset de @tfds/tokens + `@source`)                                                                                                                                                                                                                                                     | active | 2026-07-11 | Único consumidor usa Tailwind; reescrever camada de estilo não paga                                            |
| AD-003 | `meta.json` por componente é a fonte única de registry (Storybook, MCP, agentes); campos de analytics (`analyticsDefault`, `supportedEvents`, `piiRisk`) vivem nele                                                                                                                                                                               | active | 2026-07-11 | Convenção IA-first; uma fonte, três consumidores                                                               |
| AD-004 | Política "sem tag nativa" com matriz tag→primitivo e exceções (`a`, `img`, `li`, `svg`, `form`, `table`); enforcement primário via `@tfds/eslint-config`, MCP como 2ª camada                                                                                                                                                                      | active | 2026-07-11 | Guardrail estilo Alchemy; lint roda sempre, MCP só quando consultado                                           |
| AD-005 | Versionamento independente por pacote via changesets; release por GitHub Actions                                                                                                                                                                                                                                                                  | active | 2026-07-11 | Default da ferramenta; lockstep só paga em DS corporativo                                                      |
| AD-006 | Analytics segue o padrão alchemy-analytics adaptado web-only (5 eventos `component_*`, disparo automático com default por componente, provider-agnostic, no-op sem Provider)                                                                                                                                                                      | active | 2026-07-11 | Padrão validado em produção na Livelo; case de instrumentação nativa                                           |
| AD-007 | `className` permitido APENAS em primitivos de layout (VStack, HStack, Grid); proibido em componentes estilizados (Button, Badge, Input...) — resolve contradição doc×código do repo                                                                                                                                                               | active | 2026-07-11 | Layout precisa de escape (grid templates); componentes estilizados mantêm a regra "props only" do CLAUDE.md    |
| AD-008 | DS enxuto: só entra o que o portfolio precisa hoje; crescimento sob demanda                                                                                                                                                                                                                                                                       | active | 2026-07-11 | Filosofia do projeto                                                                                           |
| AD-009 | Tracker `tasks/TDS-0NN-*.md` (fora do `.specs`) e o plano formal `tfds-v2` coexistem; trabalho já feito via TDS-010/011/012 (Stack/VStack/HStack, migração do Hero) é absorvido nas tasks correspondentes do tfds-v2 (T15-T16) em vez de refeito do zero — completar apenas o que falta (meta.json, testes 1:1, className/AD-007, commit atômico) | active | 2026-07-11 | Evitar retrabalho; branch `feat/tds-011-vstack-hstack` já tinha esse trabalho ao retomar a execução do tfds-v2 |

## Handoff

- **Feature ativa:** `tfds-v2` — spec e tasks aprovadas, execução em andamento
- **Fase:** Execute — Phase 1 (Tokens, T1-T5), Phase 2 (Convenção, T6-T9), Phase 3 (Retrofit A, T10-T14) e Phase 4 (Retrofit B + novos, T15-T19) **concluídas**; Phase 5 (Guardrail + Publicação) **T20-T23 concluídas, T24 é checkpoint manual pendente (ver abaixo)**; Phase 6 (Migração do portfolio) delegada, PT1-PT3 feitos no repo `my-portfolio`, **PT4 checkpoint pendente**; Phase 7 (Analytics) **T28-T30 concluídas nesta sessão, T31-T32 bloqueadas por checkpoint do usuário (conta PostHog)**. Branch `feat/tfds-v2-phase-1` (25 commits atômicos, gate build/quick/full verde em cada task).
- **Phase 7 — o que foi feito (T28-T30, execução inline nesta sessão):** novo pacote `@tfds/analytics` (`bbd2c9e` T28) com `AnalyticsEvent`/`EventType`/`TrackAdapter` e `buildAnalyticsEvent`/`emitAnalyticsEvent` (payload com `component_name`/`screen_name`/`ds_version`, try/catch silencioso no emit — ANA-05). `ds_version` é a versão do próprio `@tfds/analytics` (lida do `package.json` via import JSON), não a de `@tfds/react` — decisão documentada porque o design não especifica a fonte e o pacote não depende de `@tfds/react`. `d4d2577` (T29) — `AnalyticsProvider({ adapter, screenName })`, `ParentComponentContext` (nesting → `parent_component` no evento, ANA-02) e `useAnalytics({ componentName, screenName? })` → `{ emit }`; sem Provider no tree, contexto default não tem adapter → no-op silencioso (ANA-04). `1dbc881` (T30) — Button (`click`) e Input (`change`) chamam `emit` via novas props `analyticsEnabled` (default `true`, reflete `analyticsDefault: "on"`), `analyticsCustomParams` e `screenName` (override do Provider); Input nunca anexa o próprio `value` ao evento — `analyticsCustomParams` é a única via de dados extras (ANA-07, piiRisk medium). `meta.json` dos 9 componentes atualizado: Button/Input → `analyticsDefault: "on"`; Badge/Label/Typography seguem `"off"` (display-only, não instrumentados nesta task); Card/Grid/HStack/VStack → `"excluded"` (primitivos de layout, nunca emitem). Testes: `@tfds/analytics` 14 (novo pacote), `@tfds/react` 130 (119 base + 11 novos entre Button/Input).
- **Verifier rodou para Phase 7 (grupo de prioridade T28-T30, não a feature inteira — T31+/Phase 8 seguem pendentes):** **PASS** ✅ — 6/6 ACs em escopo (ANA-01..05, ANA-07) com evidência `file:line`, ANA-06 fora de escopo (depende de T31); gate 144 testes passando, 0 falhas; sensor de discriminação 3/3 mutações mortas; 0 gaps. Relatório em `.specs/features/tfds-v2/validation.md`.
- **Phase 3 — o que foi feito (via sub-agent batch worker, Batch A):** retrofit de Button (`aac89b0`), Typography (`7f45213`), Label (`08fc7ac`), Badge (`2d31d0d`), Input (`7786628`) para a convenção v2 — cada um ganhou `meta.json` válido contra o schema (`analyticsDefault: "off"`, analytics é Phase 7), stories em `apps/docs/src/stories/`, e `className` removido da interface de props (`Omit<..., "className">`) conforme AD-007. Teste de foco visível adicionado só nos componentes focáveis (Button, Input via `userEvent.tab()` + classe `focus-visible:ring-interactive-focus`); Typography/Label/Badge documentados no meta.json como não-focáveis, sem teste inventado.
- **Phase 4 — o que foi feito (via sub-agent batch worker, Batch B; worker bateu o limite de sessão antes de reportar — retomado e concluído nesta sessão a partir do estado real do repo, não do relatório):** T15-T16 retrofit VStack (`b2d481d`) / HStack (`71ec6e7`) para convenção v2 com `className` mantido (AD-007, primitivo de layout). T17 `Grid` (`c8b1c9d`) — `cols` fixo ou responsivo por breakpoint (`{ base, sm, md, lg }`) + `gap`, className permitido; corrigido erro de type-check em `resolveGridCols` (index access `string | undefined` sob `noUncheckedIndexedAccess`) antes do commit. T18 `Card` (`0f2eb00`) — surface simples (`shadow: none|sm|md`, `padding: none|sm|md|lg`), sem subcomponentes (Header/Content/Footer) e sem `className` (não é primitivo de layout), API dimensionada a partir do uso real do Card do portfolio (bg/border/shadow-sm com hover:shadow-lg, padding p-6). T19 (`43a09de`) — theme switcher já existia em `apps/docs/.storybook/preview.ts` via `withThemeByClassName`, mas mapeava "Ocean" → classe `ocean` que não existe; token CSS gera `.ocean-sunset` (`packages/tokens/dist/css/themes/ocean-sunset.css`). Corrigido o mapeamento; `pnpm build` do Storybook verde com os 9 componentes.
- **Phase 5 — o que foi feito (via sub-agent Batch C nesta sessão):** T20 (`e486dbb`) — regra ESLint `no-native-tag` em `packages/eslint-config/rules/no-native-tag.js`, lendo matriz/exceções de `docs/guardrails.json` (path relativo, pacote é `private` → só roda no monorepo); registrada no preset como `tfds/no-native-tag: "error"`; RuleTester (`no-native-tag.test.js`, 25 casos: 17 tags da matriz sinalizadas com replacement correto + 6 exceções + 2 componentes DS permitidos) sob vitest; adicionadas devDeps `eslint`/`vitest` e script `test` ao pacote. T21 (`2cc84ae`) — `publishConfig.access: "public"` em tokens/icons/react, changeset inicial (`.changeset/initial-public-release.md`, minor nos 3 → 0.1.0), e **fix necessário no `scripts/verify-pack.mjs`**: só tratava export targets string; estendido para recorrer em conditional exports (objeto `{types, import, require}`), sem o qual verify-pack falhava em icons/react (done-when de T21 exige passar nos 3). verify-pack agora passa nos 3 pacotes.
- **T22 concluído (checkpoint do usuário, executado nesta sessão):** Thiago criou a org `tfds` no npmjs.com, autenticou via `npm login`, e publicou com `pnpm release` usando um Classic Automation Token (necessário porque a org exige 2FA ou token com bypass explícito — token granular sem a flag de bypass foi rejeitado com E403 antes disso). `@tfds/react@0.0.1`, `@tfds/tokens@0.0.1`, `@tfds/icons@0.0.1` publicados e validados: `npm install` em diretório limpo resolve e `require('@tfds/react').Button` importa corretamente. **Sem commit** (é publish, não código) — tags de release criadas pelo changesets (`@tfds/icons@0.0.1`, `@tfds/react@0.0.1`, `@tfds/tokens@0.0.1`). Pendência de segurança sinalizada ao usuário: reativar 2FA, revogar os tokens usados na sessão (ficaram expostos no chat), regenerar recovery codes.
- **T23 concluído (`5da226c`)** `fix(ci): map NPM_TOKEN env var to match project .npmrc convention` — `.github/workflows/release.yml` **já existia** desde o `chore: initial setup` (anterior à branch da feature), com `changesets/action@v1` e `publish: pnpm release`, mas só exportava `NODE_AUTH_TOKEN`. O `.npmrc` do projeto referencia literalmente `${NPM_TOKEN}` — o mesmo mismatch que quebrou o publish manual do T22 até exportarmos a env var certa. Adicionada `NPM_TOKEN: ${{ secrets.NPM_TOKEN }}` ao lado de `NODE_AUTH_TOKEN` no step. Gate build rodado (build ✓, test ✓ 119+25; type-check falha isolada e pré-existente em `@tfds/icons`, já documentada). **Validação end-to-end pendente** (abrir um changeset de teste, dar push/merge e conferir que a PR "Version Packages" abre e publica) — não executada porque exige push real ao GitHub e o secret `NPM_TOKEN` configurado nas Settings do repo (ação do usuário, ainda não confirmada).
- **⚠️ T24 pendente (checkpoint manual do usuário):** conectar o Storybook (`apps/docs`) na Vercel e validar URL pública com os 9 componentes nos 3 temas.
- **Nota `.npmrc`:** `.npmrc` referencia `${NPM_TOKEN}`; localmente só produz warning inofensivo quando a env var não está setada.
- **Achados durante a Phase 2 (ainda válidos, não corrigidos):** `pnpm lint` (raiz) falha com "out of memory" e `pnpm --filter @tfds/icons type-check` falha por `@types/node` ausente (em `scripts/measure-tree-shaking.ts`) — ambos confirmados **pré-existentes**, fora de escopo; o Build gate de T21 (`pnpm build && pnpm type-check && pnpm test`) ficou verde exceto por essa falha isolada de type-check do icons.
- **Suítes de teste:** `@tfds/react` 119 testes, `@tfds/tokens` 14, `@tfds/eslint-config` 25 (novo em T20) — todas passando. `pnpm test` (turbo) verde nos 6 test tasks.
- **Divisão em batches de sub-agent (acordada com o usuário para as phases 4-8, 23 tasks restantes):** Batch B = Phase 4 (T15-T19, **concluído**); Batch C = Phase 5 (T20-T24, tem checkpoint de usuário T22 npm publish e T24 Vercel); Batch D = Phase 6 (T25-T27, checkpoint T27 deploy); Batch E = Phase 7 (T28-T32, checkpoint T31 PostHog); Batch F = Phase 8 (T33-T37, checkpoint T37 publish).
- **Próximo passo:** **T24 (conectar Storybook na Vercel)** e **PT4 (deploy preview do portfolio)** são ações do usuário, ainda pendentes. **T31 (adapter PostHog) é checkpoint do usuário** (conta PostHog) — bloqueia T31-T32; até lá, Phase 7 fica parada em T30. Falta também validar o T23 end-to-end (secret `NPM_TOKEN` nas Settings do GitHub). Phase 8 (MCP, T33-T37) segue disponível para começar a qualquer momento (depende só de T22, já feito). Verifier da feature completa (todas as phases) só roda quando T31-T32 e Phase 8 fecharem.
- **AD-010 (delegação lado-portfolio):** as tasks T25–T27 (Phase 6) e T31–T32 (parte da Phase 7) passam a ser executadas a partir do repo `my-portfolio`, rastreadas em `my-portfolio/.specs/features/tfds-v2-migration/tasks.md` como PT1–PT6 (com uma task extra de adoção de Card/Grid). O Batch D aqui NÃO deve re-executá-las — ao chegar nelas, considerar "delegada" e conferir o STATE.md do portfolio. T28–T30 (analytics no DS) e Phase 8 (MCP) continuam aqui.
- **Batch D (Phase 6, delegada) — PT1-PT3 concluídos nesta sessão no repo `my-portfolio`, branch `feat/tfds-v2-migration`:** PT1 `ddb8708` (deps npm + imports renomeados; achado: `Typography`/`Badge`/`Button` do DS perderam `className` por AD-007, quebrando 43 usos no portfolio — corrigidos com wrapper + override de CSS custom property escopado), PT2 `913d15c` (Card local trocado pelo Card do DS em Experience/Skills, perdendo o hover-shadow por decisão do usuário; Grid do DS adotado em 6 grids simples), PT3 (build/lint/type-check verdes + validação visual 3 temas via Playwright headless, sem regressão, sem commit). **PT4 é checkpoint do usuário** (deploy preview na Vercel) — ver `my-portfolio/.specs/STATE.md` para o handoff completo do lado portfolio.
- **Batch F (Phase 8, MCP) — T33-T36 concluídos em 2026-07-12, em WORKTREE SEPARADO:** executados a partir da sessão do portfolio, isolados em `git worktree` na pasta `../tf.ds-phase8`, branch **`feat/tfds-v2-phase-8`** (criada de `5ddc891`), porque esta sessão (`feat/tfds-v2-phase-1`) tinha trabalho staged do analytics em andamento. Commits: T33 `9f4fe12` scaffold `packages/mcp` (`tfds-mcp`, SDK `@modelcontextprotocol/sdk@1.29`, bin ESM com shebang, teste initialize via InMemoryTransport + smoke stdio manual); T34 `e7ae2f6` bundle de dados no build (`scripts/bundle-data.mjs` copia 9 meta.json + 3 temas de tokens resolvidos + guardrails + manifest com versões do DS pra `dist/data/`; loader tipado `src/data.ts`; falha loud se fontes ausentes); T35 `793107b` as 5 tools v1 com erro estruturado + sugestões (15 testes; `createServer` passou a exigir `BundledData` — teste do T33 ajustado na assinatura, asserções intactas); T36 `ad44269` resources (`tfds://components/{name}`, `tfds://tokens/{theme}`, `tfds://guardrails`, `tfds://manifest`) + prompt `setup_project` (21 testes no total, type-check e build verdes). **Atenção ao merge:** `pnpm-lock.yaml` divergiu entre `feat/tfds-v2-phase-1` (analytics) e `feat/tfds-v2-phase-8` (mcp) — resolver regenerando com `pnpm install` após o merge. Worktree remove-se com `git worktree remove ../tf.ds-phase8` depois do merge.
- **⚠️ T37 pendente (última task da Phase 8, checkpoint do usuário):** merge da `feat/tfds-v2-phase-8` na branch principal da feature → changeset do `tfds-mcp` → publish (fluxo changesets ou manual) → conectar via `npx tfds-mcp` no Claude Code e executar as 5 tools (validação end-to-end MCP-01..05). Depois de T37 + T31-T32: rodar o Verifier da feature completa.
- **Contexto externo:** plano completo em memória do Claude (`project_tfds_v2_plan.md`); consumidor é o repo `my-portfolio` (branch develop); ver AD-009 sobre track TDS-0NN paralelo
